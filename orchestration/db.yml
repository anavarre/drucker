---
- hosts: drucker_db
  become: yes

  gather_facts: yes
  vars_files:
    - vars.yml
  handlers:
    - include: handlers.yml
  tasks:
    - include: playbooks/db/mysql.yml
    - include: playbooks/db/mysql-backup.yml
