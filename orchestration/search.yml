---
- hosts: drucker_search
  become: yes

  gather_facts: yes
  vars_files:
    - vars.yml
  handlers:
    - include: handlers.yml
  roles:
    - geerlingguy.java
    - geerlingguy.solr
  tasks:
    - include: playbooks/solr-configuration.yml
